name: Renovate

on:
  workflow_dispatch:
  schedule:
    - cron: "0 3 * * *"

jobs:
  renovate:
    runs-on: docker

    steps:
      - name: Run Renovate
        uses: docker://renovate/renovate:44.14.12-full@sha256:2dc96cd8ccd019b3aabbcff7d7894c05796db4cb98ed428611338bfa8d93c39f
        env:
          RENOVATE_PLATFORM: gitea
          RENOVATE_ENDPOINT: https://forge.superkooka.com/api/v1
          RENOVATE_TOKEN: ${{ secrets.RENOVATE }}
          RENOVATE_REPOSITORIES: ${{ forgejo.repository }}
          GITHUB_COM_TOKEN: ${{ secrets.RENOVATE_GITHUB_COM_TOKEN }}

          # Should be a read only token
          RENOVATE_HOST_RULES: |
            [
              {
                "matchHost": "forge.superkooka.com",
                "hostType": "docker",
                "username": "${{ forgejo.actor }}",
                "password": "${{ secrets.WRITE_PACKAGE }}"
              }
            ]

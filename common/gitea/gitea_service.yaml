---
apiVersion: v1
kind: Service
metadata:
  name: socat-proxy
  namespace: prod
spec:
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 80
      targetPort: http
  selector:
    app: socat-proxy

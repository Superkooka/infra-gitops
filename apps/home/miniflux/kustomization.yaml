# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base/miniflux
  - miniflux-databaseclaim.yaml
  - miniflux-secret.yaml
  - miniflux-ingress.yaml
patches:
  - path: miniflux-values-patch.yaml
    target:
      kind: Deployment
      name: miniflux

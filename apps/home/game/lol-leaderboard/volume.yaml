apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lol-leaderboard-database-pvc
  namespace: game
  annotations:
    recurring-job-group.longhorn.io/default: enabled
spec:
  persistentVolumeReclaimPolicy: Retain
  accessModes: ["ReadWriteOnce"]
  storageClassName: longhorn
  resources:
    requests:
      storage: 256Mi

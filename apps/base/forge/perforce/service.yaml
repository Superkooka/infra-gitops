apiVersion: v1
kind: Service
metadata:
  name: perforce-service
  namespace: forge
spec:
  selector:
    app: perforce-server
  ports:
    - name: perforce
      port: 1666
      targetPort: 1666
      protocol: TCP

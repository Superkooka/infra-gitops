apiVersion: v1
kind: Service
metadata:
  name: miniflux
  namespace: miniflux
  labels:
    app: miniflux
spec:
  selector:
    app: miniflux
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP

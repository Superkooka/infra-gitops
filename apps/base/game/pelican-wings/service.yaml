apiVersion: v1
kind: Service
metadata:
  name: pelican-wings-service
  namespace: game
spec:
  selector:
    app: pelican-wings
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
    - name: sftp
      port: 2022
      targetPort: 2022
      protocol: TCP

apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: database-local-cr
  namespace: flux-system
spec:
  dependsOn:
    - name: infrastructure
  interval: 10m
  path: ./cluster/home/database
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
